# Database
DATABASE_URL="postgresql://username:password@localhost:5432/fromcafe"

# NextAuth
NEXTAUTH_SECRET="your-nextauth-secret"
NEXTAUTH_URL="http://localhost:3000"

# Evernote API
EVERNOTE_CONSUMER_KEY="your-evernote-consumer-key"
EVERNOTE_CONSUMER_SECRET="your-evernote-consumer-secret"

# Image Configuration
# Default prefix for generated image filenames (default: "Alexy")
IMAGE_PREFIX="Alexy"

# Whether to extract EXIF dates for image filenames (default: true)
USE_EXIF_DATES="true"

# Maximum filename length (default: 100)
MAX_FILENAME_LENGTH="100"